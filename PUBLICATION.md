# 公开整理记录

## 保留的内容
保留 `SKILL.md`、八份分部分参考文件、双语适配说明、修改权限、交付检查表、12个测试场景和使用文档。内部标识与目录为 `management-writing`，仓库和界面显示为 `management_writing`。

核心写作要求未重新编写或简化。`07-editing-permissions.md` 保持原文件不变；`SKILL.md` 保留原有指令，仅补充公开版来源说明。“段落结构调整必须先征求用户同意”继续生效。

## 未公开的材料类别
- 三篇原始 PDF／Word 文件。
- 论文页面截图、渲染文件及原文长段摘录。
- 未确认公开状态的稿件题名、原文件名、具体场景、理论关系、研究结果、版本定位、原件哈希与基于其内容的双语改写。
- 本地私人参考目录、工作稿、凭据和环境文件。

上述文件不在公开目录中；原附件及本地私人副本保留。公开版保留自行总结的方法；已发表来源通过 DOI 与规范引用标识。来源索引只描述公开内容，不再声称包含原件。

## 完整性与使用边界
`checksums.sha256` 列出所有公开文件（自身除外），以仓库根目录为路径基准。执行 `shasum -a 256 -c checksums.sha256` 可检查完整性。

Skill 的全部必读相对链接指向随仓库提供的 Markdown 文件，不要求访问私人原件。公开文献定位仅用于可选核查，不保证不同版本的 PDF 页序相同。

`.gitignore` 排除私人参考目录、稿件、原始 PDF／Word、截图与 ZIP。忽略规则不能移除已经进入 Git 历史的文件；后续提交前仍须检查待提交内容。

没有添加 MIT、Apache 或其他开放许可证。技术完整性核验不等于已完成行为测试、客户端安装或用户成稿验收；测试表中的待运行状态保持诚实。
